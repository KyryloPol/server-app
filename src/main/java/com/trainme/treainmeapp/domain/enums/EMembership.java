package com.trainme.treainmeapp.domain.enums;

/**
 * Creating a new enum called EMembership.
 * ROLE_STANDARD is standard user role and ROLE_ADMIN is admin user role.
 * <p>
 * It will depend on what requests the user can send to API and what can see on the web page app.
 */
public enum EMembership {
    ROLE_STANDARD,
    ROLE_ADMIN,
}
